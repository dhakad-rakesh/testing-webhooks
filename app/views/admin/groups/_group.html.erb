<tr class="gradeX">
  <td><%= group.id %></td>
  <td><%= group.name %></td>
  <td>
    <%= link_to t('edit'), edit_admin_group_path(group) %>
    <%= link_to t('show'), admin_group_path(group) %>
    <%= link_to t('destroy'), admin_group_path(group),
              method: :delete,
              data: { confirm: t('data_confirm_dialog') } %>
  </td>
</tr>
